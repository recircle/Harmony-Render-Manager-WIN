# Harmony-Render-Manager-WIN
Harmony Render Manager Win version in C#

GUI for ToonBoom Harmony batch render. It modifies the xstage (xml) document, insert video export settings and duplicates original document. The plan is to includes the ability to select the type of export (video or image sequence) and individual elements/characters.

